services:
  - type: web
    name: lazycv-backend
    runtime: node
    rootDir: Backend
    buildCommand: npm install && npx puppeteer browsers install chrome
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
